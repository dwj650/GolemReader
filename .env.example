# Copy to ".env" (which is gitignored) and fill in real values. Never commit the real .env.
# NAME=value  (no spaces around =, no quotes needed)
[FILL during setup: add the secrets/config keys this project needs]
