# Archive — baselines + snapshots (NEVER overwritten)
Per-phase baselines under `V{n}-P{n.m}/` (test results, build metadata, screenshots vs
prototype). A newer baseline supersedes the old with a pointer. Full project snapshots
are written at each version release.
